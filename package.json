{
  "scripts": {
    "dev": "npm run build -- --sourcemap=inline --watch",
    "build": "esbuild js/tree.js --minify --format=esm --bundle --loader:.png=base64 --loader:.gif=base64  --outdir=src/awitree/static"
  },
  "dependencies": {
    "jquery": "^3.7.1",
    "jstree": "^3.3.17",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
